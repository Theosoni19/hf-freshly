// Fichier Js personnalisé

$(document).ready(function(){
    // Initialisation de Slick Slider
    $(".carousel-wrapper").slick();
    
    // Initialisation de Mixitup
    let mixer = mixitup("#mixer-container");
})
